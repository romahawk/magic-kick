# Next Session Start

**Last updated:** 2026-03-06

## Start Here

1. Read `README.md` for current product framing and setup commands.
2. Read `docs/SPRINT_BACKLOG.md` and pick one active issue outcome.
3. Check local branch and working tree cleanliness with `git status --short`.
4. Run `npm run lint` and `npm run build` before making changes.
5. Update `CHANGELOG.md` and docs if the task changes user-facing behavior or workflow policy.

## Current Focus

- Repository compliance hardening
- CI/policy workflow coverage
- README structure cleanup

## Watchouts

- Do not commit generated files like `tsconfig.tsbuildinfo`.
- Do not stage unrelated local assets during a scoped fix.
- Do not commit if lint or build is failing.
